# 更新日志

## v0.5.0（2026-09-25）

- 修复：指令词之间带空格就不被识别——中文输入法常把“鉴图 开启”“鉴图 状态”“鉴图 概率 50”“鉴图 榜”自动加空格，此前会当成普通消息并回一句“请回复一张图片”，现在全部正常识别。
- 修复：图片下载没有流式体积上限——此前用 `response.read()` 一次性读完整个响应体，`max_image_mb` 只在读完之后才校验，指向超大文件（或恶意 URL）的响应会先把内容全读进内存。现在边下边累计体积、超限立即中断，并会先看 `Content-Length` 直接拒绝明显超标的响应。
- 修复：data URL 声明的 MIME 可能与实际内容不符——压缩失败时会把 PNG/BMP 等原始字节声明成 `image/jpeg`，个别严格的模型网关可能拒收；现在按实际字节魔数决定。同时清理了从未被读取的 `NormalizedImage.mime` 字段。
- 新增：个人战绩——`鉴图榜 @某人` 或 `我的鉴图` 查看被评次数、平均分、最高/最低分，支持 `本周` / `总` 范围，所有人可查。
- 新增：重复图结果缓存——同一张图（按内容匹配，不受 URL 变化影响）在 `result_cache_seconds` 有效期内重复鉴图直接复用上次结果，不再调用模型，省额度；设 0 关闭。
- 新增：自动触发免打扰时段——`quiet_hours` 配置如 `23:00-08:00`（支持跨零点），时段内不自动触发，避免半夜刷屏；关键词触发是用户主动发起，任何时段都照常响应。
- 新增：每群每小时自动触发上限——`group_hourly_limit`，配合现有冷却防某个群刷屏消耗额度；只统计自动触发，手动触发不计入，且取到图才占配额。
- 优化：`鉴图状态` 现在同时展示每小时上限与免打扰时段设置。

## v0.4.0（2026-08-29）

- 修复：触发词“评价 / 点评”等高频词出现在普通聊天里（如“帮我评价一下这个方案”）会误触发鉴图并拦截主 agent 处理该消息——现在无图片上下文（不带图、无回复、无 @目标）时，只有消息基本就是一条鉴图指令（如“打分”“打分 毒舌”）才触发，正常聊天不再被打扰。
- 修复：无回复消息时也会扫描原始消息 JSON 提取图片 URL，导致用户在文本里手打的任意 URL（含内网地址）被下载——现在只有消息确实包含回复时才做这层兜底提取。
- 修复：“鉴图概率 abc”此前会回退成普通鉴图触发并提示“请回复一张图片”，现在会正确提示概率用法；“鉴图概率50”（无空格）也能识别。
- 新增：本群鉴图排行榜——`鉴图榜` 查看今日高分榜（附“最惨”记录），`鉴图榜 本周` / `鉴图榜 总` 切换时间范围，所有人可查；记录按群持久化到插件数据目录（每群最多保留最近 500 条）。

## v0.3.0（2026-08-22）

- 新增：头像鉴图——发送“锐评 @某人”（无图片时），自动抓取对方 QQ 头像打分吐槽，主要支持 QQ 平台，可用 `enable_avatar_judge` 关闭。
- 新增：群管理指令——`鉴图开启` / `鉴图关闭` / `鉴图概率 0-100` / `鉴图状态`，群主、群管理员或 Bot 管理员可按群调整自动鉴图（JSON 持久化到插件数据目录）；全局 `enable_auto_trigger` 仍是总闸。
- 修复：`private_enable`（允许私聊使用）配置此前未生效，现关闭后私聊不再触发鉴图。
- 修复：自动触发曾被纯文本消息白白消耗群冷却——现在只对可能带图（或回复）的消息掷概率，且确认取到图片后才计入群冷却。
- 修复：自动触发在失败时（冷却/配额提示、下载失败、模型报错等）不再往群里插话，保持静默只记日志；自动触发也不再消耗发图者的个人冷却与每日额度。
- 修复：`data:` URI 图片此前绕过大小上限与压缩，现与其他引用走同一套归一化管线（大小限制、魔数识别、GIF 处理）。
- 修复：评分卡片对模型输出的理由/吐槽做 HTML 转义，模型输出含 HTML 标签时不再破坏卡片排版。
- 修复：部分平台 `Image.file` 只是缓存文件名时会挡住可用的 `url`，现自动回退。
- 更新：`temperature` 配置类型从 string 改为 float；文档与安装说明修正。

## v0.2.0（2026-08-15）

- 修复：回复图片消息 + 发送打分指令时取不到图片的问题。新增三级取图策略——优先读 AstrBot 已填充的 `Reply.chain`，其次从原始消息 JSON 递归挖掘，最后用 OneBot `get_msg` 动作兜底。
- 新增：评分卡片五种视觉主题（霓虹 / 极简 / 复古 / 赛博朋克 / 马卡龙），可在配置项 `card_theme` 设置默认主题，也可在命令中临时指定（如“打分 复古”）。
- 更新：替换插件图标。

## v0.1.0（2026-08-15）

- 首个版本：AI 鉴图评分插件。
- 关键词触发：回复图片（或当前消息带图）后发送“打分 / 鉴图 / 点评 / 评价 / 锐评”触发鉴图，可附带风格词（如“打分 毒舌”）。
- 自动触发：可选的“对群内所有图片自动鉴图”开关，带概率、群白名单、冷却控制。
- 输出：默认纯文本（分数 + 理由 + 吐槽），可切换渲染评分卡片图。
- 三种吐槽风格：毒舌 / 客观 / 彩虹屁，支持自定义系统提示词。
- 防刷：按用户冷却 + 每日上限；忽略表情包 / GIF（可配置）。
